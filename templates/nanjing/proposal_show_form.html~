<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
<title>{{appitem.name}}</title>
<link href="/static/nanjing/css/base.css" rel="stylesheet" type="text/css" />
</head>

{% load tag_extras %}
<body>
	<div class="wrap">
    	<dl class="list_box" style="padding:30px;">
           <dt style="font-size:40px;">{{proposal.title}}</dt>
           <dt style="font-size:9px;">{{proposal.create_time}}</dt>
           {% if proposal.content %} <dd><b>内容：</b>{{proposal.content|safe}}</dd> {% endif %}
           {% if proposal.submitter %} <dd><b>提交人：</b>{{proposal.submitter}}</dd> {% endif %}
           {% if proposal_seconded_num %} <dd><b>附议人数：</b>{{activity.xingshi}}</dd> {% endif %}
           {% if activity.place %} <dd><b>地点：</b>{{activity.place}}</dd> {% endif %}
           {% if activity.speaker %} <dd><b>主讲：</b>{{activity.speaker}}</dd> {% endif %}
           {% if activity.count %}  <dd><b>人数：</b>{{activity.count}}</dd> {% endif %}
           {% if activity.activity_images %} <dd><b>图片:</b></dd>{% endif %} 
            <dd style="height:30px;"><button style="float:right;width:100px;height:45px;"
 onclick="window.location.href='http://{{appitem.domain}}{% url 'nanjing:activity_user' appitem.slug activity.id %}'">报名</button></dd>
           <table>
           <tr>
           {% for image in activity.activity_images.all %}
           <td style="listimg">   <img src="{{image.get_image_url}}" height="180px"/> </td>
           {% endfor %}
           <tr>
           <table>
        </dl>
    </div>
</body>
</html>
